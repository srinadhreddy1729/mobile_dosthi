import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'android_application_dosthi',
  webDir: 'www',
  "server": {
    "iosScheme": "http",
    "androidScheme": "http"
  }
};





export default config;
